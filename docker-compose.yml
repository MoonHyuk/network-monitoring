version: "3.7"

networks:
  network:
    driver: bridge

services:
  app:
    image: network_monitoring:0.1
    container_name: network_monitoring
    build:
      context: .
      dockerfile: Dockerfile
    networks:
      - network
    environment:
      - hosts=google.com,naver.com,127.0.0.1:8080